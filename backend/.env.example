# Server
PORT=5000
CLIENT_URL=http://localhost:5173

# Database
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=cctv_health_monitoring

# Auth
JWT_SECRET=
JWT_EXPIRES_IN=15m

JWT_REFRESH_SECRET=
JWT_REFRESH_EXPIRES_IN=7d

# Nodemailer (technician temp password email)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
